development:
  adapter: async
  AWS_ACCESS_KEY_ID: password_nur
  AWS_SECRET_ACCESS_KEY: password_xan

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: blog_demo_production
  AWS_ACCESS_KEY_ID: password_nur
  AWS_SECRET_ACCESS_KEY: password_xan
